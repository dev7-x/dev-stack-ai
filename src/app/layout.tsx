import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dev Stack AI | 7-Day MVP Expert",
  description: "Expert Product Architect & Full-stack Developer helping you build modern SaaS, AI-powered systems, and high-performance data pipelines. I turn your business into a MONSTER UPGRADE with proven speed and technical depth.",
  icons: {
    icon: "/dev-stack-ai-logo2.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
